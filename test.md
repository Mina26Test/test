# Project Alpha: Markdown Test

## Overview
This is a **Markdown** file used for testing formatting. Markdown is popular for its simplicity and wide support in tools like GitHub and VS Code.

### Features
* **Lightweight**: Easy to read as plain text.
* **Portable**: Works almost everywhere.
* **Support**: Handles `inline code` and blocks.

---

### Sample Code
```python
def greet(name):
    print(f"Hello, {name}!")

greet("World")